# Activity Summary for 4/17/2026

## 4:26:17 PM
The provided log details changes to two files: `/Users/zhihao/Desktop/HTX/kimiK2.5_test.py` and `/Users/zhihao/Desktop/HTX/.env`.

For `/Users/zhihao/Desktop/HTX/kimiK2.5_test.py`:

**Key Changes and Timestamps:**

*   **Initial State (4/17/2026, 2:09:35 PM):** The script is a Kimi-K2.5 inference test for a SageMaker Endpoint. It includes a `Metrics` class to track latency (TTFT), throughput, and total duration. It defines an `invoke` function for both streaming and non-streaming requests, and a `print_response` function for non-streamed outputs. Three main test cases are outlined, involving "Instant Mode (Non-Thinking, Non-Streaming)", "Thinking Mode (Non-Streaming)", and "Streaming — Non-Thinking (Instant)". The script uses `ENDPOINT_NAME` from environment variables.

*   **Minor Edits (4/17/2026, 2:45:57 PM and 2:51:11 PM):** These two entries show very minor, almost negligible changes. The primary discernible change is the addition of comments to `temperature` and `top_p` parameters in `payload_instant` on the 2:45:57 PM timestamp, explaining their purpose. The 2:51:11 PM entry is identical to the previous one, suggesting no functional change or an accidental save.

*   **Commenting Out Test Cases (4/17/2026, 3:08:06 PM):** Test cases 3 (Streaming — Non-Thinking) and its associated `payload_stream_instant` definition are commented out.

*   **Further Commenting Out (4/17/2026, 3:24:33 PM and 3:25:33 PM):** Test cases 1 and 2, along with their respective payloads and print calls, are also commented out. By 3:25:33 PM, almost all executable test logic in the main part of the script is commented.

*   **Introduction of `IMG_B64` and Endpoint Specificity (4/17/2026, 3:40:51 PM):** A new environment variable `IMG_B64` is introduced and retrieved. However, the `invoke` function still uses the generic `ENDPOINT_NAME` which is not explicitly defined in the script after this change, but rather fetched from the `.env` file. The `print` statement `print(f"Using endpoint: {ENDPOINT_NAME}")` remains, indicating an intent to use an endpoint, but the actual endpoint used in `invoke` functions is not explicitly defined within the script itself in this version.

*   **Refinement of Endpoint Usage (4/17/2026, 3:41:37 PM):** The `print(f"Using endpoint: {ENDPOINT_NAME}")` line is removed. The script is now consistently relying on the `ENDPOINT_NAME` variable fetched from the environment.

*   **Introducing Kimi and Qwen Endpoints (4/17/2026, 3:44:38 PM, 4:09:05 PM, and 4:09:59 PM):**
    *   The `ENDPOINT_NAME` variable is renamed to `KIMI_ENDPOINT_NAME` when fetching from `os.getenv()`.
    *   A new `QWEN_ENDPOINT_NAME` is introduced, also fetched from `os.getenv()`.
    *   The `invoke` function is modified to use `QWEN_ENDPOINT_NAME` for streaming calls and `KIMI_ENDPOINT_NAME` for non-streaming calls. This implies a strategy to test different models based on the streaming preference.
    *   The `print(f"Using endpoint: {ENDPOINT_NAME}")` line is reintroduced at 3:44:38 PM, but then removed again at 3:46:36 PM, suggesting indecision or refactoring of logging. The `invoke` function consistently uses `QWEN_ENDPOINT_NAME` for streaming and `KIMI_ENDPOINT_NAME` for non-streaming calls across these later changes.

**Patterns and Recurring Elements:**

*   **Metric Tracking:** The `Metrics` class remains consistent throughout the changes, indicating a stable and core component for performance evaluation. It consistently tracks `latency_s` (TTFT or full response), `total_duration_s`, `decode_duration_s`, `prompt_tokens`, `completion_tokens`, and `total_tokens`.
*   **Endpoint Interaction:** The script consistently interacts with SageMaker endpoints using `boto3.client("sagemaker-runtime")`. The `invoke_endpoint_with_response_stream` is used for streaming, and `invoke_endpoint` for non-streaming.
*   **Environment Variable Dependence:** Key configurations like AWS credentials and endpoint names are consistently loaded from `.env` files, demonstrating a secure and flexible configuration approach.
*   **Test Case Structure:** Despite being commented out, the structure for defining test payloads (`messages`, `max_tokens`, `temperature`, `top_p`, `stream`, `chat_template_kwargs`) and how they are invoked and their responses printed remains a consistent pattern in the script's design.
*   **Streaming Logic:** The `invoke` function contains robust logic for handling streaming responses, parsing `data:` chunks, identifying `[DONE]` messages, and tracking token counts and first-token arrival times.
*   **Commenting Practices:** There's a notable pattern of commenting out entire test blocks using multiline string comments (`""" ... """`) rather than deleting them, suggesting a temporary disabling for focus on other parts or for future re-enabling.

In summary, the `kimiK2.5_test.py` script evolved from a general SageMaker endpoint test script to one specifically configured to test Kimi and Qwen models, distinguishing between streaming and non-streaming behaviors for different endpoints. Most of the explicit test execution logic was commented out over time, while the core `Metrics` class and endpoint interaction mechanisms remained stable and were adapted for the new multi-model testing setup.