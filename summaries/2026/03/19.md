# Activity Summary for 3/19/2026

## 3:46:10 PM
The code changes primarily involve two Python files related to a Document Tampering Detection (DTD) model.

The file `/Users/zhihao/Desktop/HTX/DTD model/DocTamper/models/eval_dtd.py`, last modified on 3/19/2026, 10:48:44 AM, is an evaluation script for a deep learning model.
*   **Imports:** It uses a wide array of libraries for image processing (OpenCV, PIL, torchvision, albumentations), deep learning (PyTorch, segmentation_models_pytorch, torch.cuda.amp), data handling (LMDB, pickle, jpegio), and utilities (numpy, argparse, tqdm, logging).
*   **Argument Parsing:** It sets up command-line arguments for `data_root`, `pth` (path to the model checkpoint), `lmdb_name`, and `minq` (minimum quality factor).
*   **`TamperDataset` Class:** This custom PyTorch `Dataset` loads images and labels from LMDB databases.
    *   It incorporates transformations like horizontal/vertical flips and normalization.
    *   It uses pre-computed quantization tables stored in a `qt_table.pk` file.
    *   **Significant Update:** It includes fixes for compatibility with Azure ML studio, specifically in how `pk_path` and `lmdb_path` are constructed, adapting to how datasets might be mounted or named in that environment.
    *   The `__getitem__` method handles loading images, converting them to grayscale, applying multiple JPEG compression levels (based on `q2`, `q1`, `q` from `record`), and extracting Discrete Cosine Transform (DCT) coefficients using `jpegio`. The original RGB image is preserved for a separate input channel.
*   **Utility Functions:** It defines standard utilities like `get_logger`, `AverageMeter`, `second2time`, and `IOUMetric` for evaluating segmentation performance.
*   **Model Evaluation (`eval_net_dtd`):**
    *   The script loads a `seg_dtd` model and supports `DataParallel` if multiple GPUs are available.
    *   **Significant Update:** The model checkpoint loading mechanism was made more robust to handle different checkpoint structures, specifically extracting the `state_dict` and stripping the "module." prefix from keys, which is common when loading models saved from a `DataParallel` wrapper.
    *   It iterates through the test data, performs inference, calculates precision, recall, and updates the `IOUMetric` for segmentation tasks, finally printing these metrics along with F1-score.

The file `/Users/zhihao/Desktop/HTX/DTD model/DocTamper/check_pk.py`, last modified on 3/19/2026, 3:21:12 PM, is a utility script.
*   **Purpose:** Its sole purpose is to inspect the contents of a pickle file named `qt_table.pk`. This file is notably used by the `eval_dtd.py` script.
*   **Functionality:** It loads the pickle file, identifies the type of the loaded object (dictionary, NumPy array, list, tuple, or other), and then pretty-prints its structure and a preview of its values. For dictionaries, it shows keys and samples of values; for NumPy arrays, it shows shape, dtype, and contents; for lists/tuples, it shows length and first few items.

**Patterns and Recurring Elements:**
*   Both files were modified on the same day (March 19, 2026), suggesting a concentrated period of development or debugging.
*   The `check_pk.py` script acts as a debugging/inspection tool for a data file (`qt_table.pk`) that is a dependency for the main `eval_dtd.py` evaluation script, highlighting a focus on understanding and validating data inputs.
*   The `eval_dtd.py` script shows a strong emphasis on robust data loading, complex image processing involving multiple JPEG compression steps and DCT coefficient extraction, and robust model loading (handling DataParallel prefixes). The fixes for Azure ML compatibility indicate an environment where portability and specific platform requirements are important.

## 4:46:12 PM
The provided code changes relate to a "DocTamper" model, focusing on document tampering detection through analysis of JPEG compression artifacts. Both updates occurred on March 19, 2026.

**`/Users/zhihao/Desktop/HTX/DTD model/DocTamper/models/eval_dtd.py` (Timestamp: 3/19/2026, 10:48:44 AM)**
This file is the core evaluation script for the document tampering detection model.
*   **Imports:** It features a comprehensive set of imports covering deep learning frameworks (PyTorch, `segmentation_models_pytorch`), image processing (OpenCV, PIL, `jpegio`, `albumentations`), data handling (`lmdb`, `pickle`), and utilities (`argparse`, `logging`, `tqdm`).
*   **Argument Parsing:** Configures command-line arguments for data root, model path (`.pth`), LMDB database name, and minimum JPEG quality (`minq`).
*   **`TamperDataset` Class:**
    *   Designed for loading image and label data from an LMDB database.
    *   Loads quantization tables from `qt_table.pk` and records from specific pickle files, including an **adaptation for Azure ML Studio** to correctly resolve dataset paths.
    *   In `__getitem__`, it retrieves images and masks, performs multi-stage JPEG recompression on the image using different quality factors (`q`, `q1`, `q2`), extracts Discrete Cosine Transform (DCT) coefficients using `jpegio`, and applies various image augmentations and normalization.
*   **Utility Functions:** Includes standard utilities for logging (`get_logger`, `inial_logger`), measuring average values (`AverageMeter`), and formatting time (`second2time`).
*   **`IOUMetric` Class:** Implements methods to calculate Intersection Over Union (IOU) and related metrics (accuracy, mean IOU, frequency-weighted IOU) for segmentation tasks.
*   **Model Evaluation (`eval_net_dtd` function):**
    *   Initializes the `seg_dtd` model and supports `DataParallel` for multi-GPU setups.
    *   Loads the model's state dictionary from a specified `.pth` file, with a **specific fix** to handle potential `module.` prefixes when loading models saved from `DataParallel`.
    *   Iterates through the test data, performs forward passes, and calculates precision, recall, and IOU metrics based on the model's predictions. The model takes image data, DCT coefficients, and quantization scales as input.

**`/Users/zhihao/Desktop/HTX/DTD model/DocTamper/check_pk.py` (Timestamp: 3/19/2026, 3:21:12 PM)**
This is a utility script dedicated to inspecting the contents of a pickle file, specifically `qt_table.pk`.
*   It loads the `qt_table.pk` file.
*   It then pretty-prints the type of the loaded object.
*   If the object is a dictionary, it lists top-level keys and then provides a detailed preview of each value, including its type, shape, data type (for NumPy arrays), length (for lists/tuples), and initial elements.
*   It also handles cases where the loaded object is a NumPy array, list, tuple, or any other type, displaying relevant information.

**Patterns and Recurring Elements:**
*   **`qt_table.pk`:** Both files interact with `qt_table.pk`; `eval_dtd.py` uses it to retrieve quantization tables, while `check_pk.py` is designed to analyze its structure. This file likely stores crucial information about JPEG quantization.
*   **Pickle Serialization:** `pickle` is heavily used across both scripts for loading configuration, data records, and model weights.
*   **JPEG Artifact Analysis:** `eval_dtd.py` explicitly handles JPEG compression quality and extracts DCT coefficients, indicating a focus on detecting manipulation by analyzing JPEG specific data.
*   **LMDB:** `eval_dtd.py` utilizes LMDB for efficient data storage and retrieval.
*   **Timestamp Proximity:** Both changes occurred on the same day, suggesting a concentrated effort on developing and debugging components related to the DTD model and its data handling.